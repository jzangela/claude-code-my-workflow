---
paths:
  - "Quarto/**/*.qmd"
---

# No Incremental Fragments in Slides

**Never use `::: {.incremental}`, `. . .`, `{.fragment}`, or any overlay/fragment commands.**

Use multiple slides for progressive builds, color emphasis for attention, and visual hierarchy for pacing. If a review agent suggests adding incremental fragments, ignore the recommendation.
