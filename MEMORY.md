# Project Memory

Corrections and learned facts that persist across sessions.
When a mistake is corrected, append a `[LEARN:category]` entry below.

---

## Project Decisions (2026-02-14)

- **Quarto-first workflow** — `.qmd` is the single source of truth; no Beamer/LaTeX
- **Course:** Graduate Regression Analysis for psychology/social sciences
- **Visual standards:** Clean academic default — white bg, 300 DPI, sans-serif
- **Color palette:** Navy (#003366), Steel Blue (#4682B4), Gray (#6B7280), Gold (#D4A843)
- **No incremental fragments** — use multiple slides for progressive builds
- **Notation convention:** $Y_i$ (outcome), $X_i$ (predictor), $\beta$ (coefficients), $\varepsilon_i$ (error)
- **R conventions:** `here::here()` paths, `set.seed(12345)` for stochastic code

---

<!-- Append new entries below. Most recent at bottom. -->
